let selectedColors = ['red' , 'blue'];

//Dynamic array (size and objects)
selectedColors[2] = 1;

console.log(selectedColors);

// Specify the index 

console.log(selectedColors[1]);

console.log(selectedColors.length);
